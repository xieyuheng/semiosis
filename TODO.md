写项目初始的 diary

为 agent.js package 增加文学式编程文档

refactor ModelConfig

- 用配置文件处理不同类型的 ModelConfig

考虑消除对 open-ai 的依赖，直接用 fetch

setup ide.js package -- 支持 markdown 的极简 IDE
setup semiosis.js package (coding agent)
